apiVersion: v2
appVersion: "1.5.1"
description: >-
  Gateway API CRDs (standard channel, bundle v1.5.1) delivered as a Helm chart so
  they are lifecycle-managed rather than applied out of band. Required by the
  gateway-api chart and any Gateway controller (e.g. nginx-gateway-fabric); install
  this before them. Vendored verbatim from kubernetes-sigs/gateway-api.
name: gateway-api-crds
version: 0.1.0
sources: ["https://github.com/RADAR-base/radar-helm-charts/tree/main/charts/gateway-api-crds"]
deprecated: false
type: application
maintainers:
  - email: mani.thumu@kcl.ac.uk
    name: Mani Thumu
