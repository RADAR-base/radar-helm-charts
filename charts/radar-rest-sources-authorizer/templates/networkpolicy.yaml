{{- if .Values.networkpolicy }}
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: {{ template "radar-rest-sources-authorizer.fullname" . }}
  labels:
{{ include "radar-rest-sources-authorizer.labels" . | indent 4 }}
spec:
  podSelector:
{{ include "radar-rest-sources-authorizer.labels" . | indent 4 }}
    {{- tpl (toYaml .Values.networkpolicy) .  | nindent 2 }}
{{- end -}}
